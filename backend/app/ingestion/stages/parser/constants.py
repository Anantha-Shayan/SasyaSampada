PARSER_NAME = "composite"
PARSER_VERSION = "1.0.0"

# Minimum characters from PyMuPDF to treat a page as text-based
DEFAULT_MIN_TEXT_CHARS = 30

# Fraction of page height for header/footer bands
DEFAULT_HEADER_FRACTION = 0.12
DEFAULT_FOOTER_FRACTION = 0.12

# Render DPI for OCR rasterization
OCR_RENDER_DPI = 200
